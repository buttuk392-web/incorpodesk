(()=>{const getSlug=()=>document.body.dataset.slug||new URLSearchParams(location.search).get('slug')||posts[0].slug;const article=posts.find(p=>p.slug===getSlug());const card=p=>`<article class="card post-card reveal"><img class="post-image" loading="lazy" src="${p.image}" alt=""><div class="post-copy"><span class="post-tag">${p.category}</span><h2>${p.title}</h2><p>${p.description}</p><div class="post-meta"><span>${p.date}</span><span>${p.time}</span></div><a class="read-link" href="blog/${p.slug}/">Read More →</a></div></article>`;const setupSubscribe=()=>document.querySelector('#subscribe-form')?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('#subscribe-result').textContent='Thanks! Your free LLC guide is on its way.';e.target.reset()});if(document.querySelector('#blog-grid')){const grid=document.querySelector('#blog-grid'),search=document.querySelector('.blog-search');let category='All',visible=6;const render=()=>{const found=posts.filter(p=>(category==='All'||p.category===category)&&(`${p.title} ${p.description} ${p.category}`).toLowerCase().includes(search.value.toLowerCase()));grid.innerHTML=found.slice(0,visible).map(card).join('')||'<p class="empty">No articles found. Try another search.</p>';document.querySelector('#load-more').style.display=found.length>visible?'block':'none';grid.querySelectorAll('.reveal').forEach(x=>x.classList.add('show'))};document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{category=b.dataset.category;visible=6;document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');render()});search.addEventListener('input',()=>{visible=6;render()});document.querySelector('#load-more').onclick=()=>{visible+=6;render()};render()}if(article&&document.querySelector('#article-content')){document.title=`${article.title} | IncorpoDesk`;document.querySelector('#meta-description').content=article.description;document.querySelector('#og-title').content=article.title;document.querySelector('#og-description').content=article.description;document.querySelector('#article-category').textContent=article.category;document.querySelector('#article-title').textContent=article.title;document.querySelector('#article-date').textContent=article.date;document.querySelector('#article-time').textContent=article.time;const image=document.querySelector('#article-image');image.src=article.image;image.alt=article.title;const content=document.querySelector('#article-content');content.innerHTML=article.content+`<div class="share"><button data-share="linkedin">Share on LinkedIn</button><button data-share="copy">Copy link</button></div><section><h2>Related insights</h2><div class="grid blog-grid">${posts.filter(p=>p.slug!==article.slug).slice(0,3).map(card).join('')}</div></section>`;const toc=document.querySelector('#toc');content.querySelectorAll('h2').forEach((h,i)=>{h.id='section-'+i;toc.innerHTML+=`<a href="#${h.id}">${h.textContent}</a>`});content.querySelectorAll('[data-share]').forEach(b=>b.onclick=()=>{if(b.dataset.share==='copy'){navigator.clipboard?.writeText(location.href);b.textContent='Link copied'}else window.open('https://www.linkedin.com/sharing/share-offsite/?url='+encodeURIComponent(location.href),'_blank','noopener')});const schema={"@context":"https://schema.org","@type":"Article",headline:article.title,description:article.description,datePublished:'2026-09-10',author:{"@type":"Organization",name:'IncorpoDesk Team'},image:article.image,mainEntityOfPage:location.href};const tag=document.createElement('script');tag.type='application/ld+json';tag.textContent=JSON.stringify(schema);document.head.append(tag)}setupSubscribe()})();
